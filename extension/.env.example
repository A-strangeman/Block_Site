# Example environment variables for the browser extension build
# Copy this to .env and fill in values before building or packaging the extension

# Base URL for backend API (no trailing slash)
API_BASE=https://api.example.com

# Feature flags
ENABLE_ANALYTICS=false
